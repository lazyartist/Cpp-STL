
1. 시퀸스 컨테이너와 연관 컨테이너의 가장 큰 차이점
2. 연관 컨테이너의 기본 정렬 기준
3. set에 같은 원소(key)를 저장하면?
4. multiset에 같은 원소(key)를 저장하면?
5. set과 map의 차이점
6. 연관 컨테이너의 lower_bound()와 upper_bound()의 반환값은?
7. 시퀸스 컨테이너의 insert()와 연관 컨테이너의 insert()의 차이점
8. 연관 컨테이너의 찾기 관련 함수의 검색 성능은?


<details>
<summary>정답</summary>
<ol>
<li>시퀸스 컨테이너는 원소간 상대적 순서가 유지되고 연관 컨테이너는 특정 정렬 기준에 의해 정렬된다.</li>
<li>< 연산자 함수 객체인 less<></li>
<li>set은 중복을 허용하지 않으므로 무시되고 반환값으로 pair객체가 반환되어 삽입 성공 여부를 알 수 있다.</li>
<li>multiset은 중복을 허용하기 때문에 정렬 기준에 따라 원소가 저장된다.</li>
<li>set은 키만 저장하고 map은 키와 값을 저장한다.</li>
<li>lower_bound: 찾은 원소의 순차열의 첫 번째를 가리킨다<br>
upper_bound: 찾은 원소의 순차열의 끝을 가리킨다.(찾은 원소 다음이기 때문에 사용할수는 없다.)</li>
<li>시퀸스의 insert: 특정 위치에 원소를 삽입<br>
연관의 insert: 특정 정렬 기준에 따라 원소의 위치를 찾아서 삽입</li>
<li>로그 시간</li>
</ol>
</details>


